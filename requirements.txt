flask>=3.0
werkzeug>=3.0      # explicit: password hashing for admin auth (was only a transitive flask dep)
waitress>=3.0      # production WSGI server (no dev-server warning)
openpyxl>=3.1      # XLSX mapping/history exports
